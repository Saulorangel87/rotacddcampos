SERVER_PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=rotas_entrega
DB_SSLMODE=disable
JWT_SECRET=troque-por-um-valor-aleatorio-longo-em-producao
CORS_ORIGINS=http://localhost:5173
JWT_EXPIRACAO_HORAS=3
# URL do Worker do Cloudflare que faz proxy pro Groq (ex:
# https://flat-rice-6724.sauloleonardo1987.workers.dev). A chave da Groq
# fica só no Worker, nunca aqui. Opcional — sem isso, o resto do site
# funciona normal, só o chat do Zé Rota fica desligado.
ZE_ROTA_WORKER_URL=
